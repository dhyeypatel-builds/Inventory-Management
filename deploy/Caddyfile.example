# Copy this to /etc/caddy/Caddyfile on the server.
# Replace the domains with your actual domains.
# Caddy handles HTTPS automatically — no certbot needed.

tyrestock.app {
    reverse_proxy localhost:3000
}

dev.tyrestock.app {
    reverse_proxy localhost:3010
}
